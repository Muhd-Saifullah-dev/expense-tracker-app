import { View, Text } from "react-native";

export default function AddScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-foreground text-xl">Add Transaction</Text>
    </View>
  );
}